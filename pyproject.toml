[project]
name = "laser-killer"
version = "0.1.0"
description = "双目视觉激光跟踪系统"
requires-python = ">=3.11"

dependencies = [
    "numpy>=2.2.0",
    "opencv-python>=4.12.0",
    "spidev>=3.8",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"